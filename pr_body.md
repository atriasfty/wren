## Requires Manual Review

- **TODO [SECURITY]**: upgrade `tar` — breaking change or deeply nested transitive dependency via `node-pre-gyp`, requires manual review.
- **TODO [SECURITY]**: upgrade `vite`, `vite-node`, `vitest` — breaking change, requires manual review (major bump from 1.6.0 to 4.x.x).
- **TODO [SECURITY]**: upgrade `sharp` — breaking change, requires manual review (inherited vulnerabilities in libvips).
